{
  "name": "shop-db-server",
  "version": "1.0.0",
  "description": "Shop API server (with MongoDB)",
  "main": "./src/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon -L ."
  },
  "keywords": [
    "api",
    "mongo",
    "shop"
  ],
  "author": "Laia Guirado Campmany",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "mongoose": "^6.3.4",
    "morgan": "^1.10.0"
  }
}
